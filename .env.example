# App
NODE_ENV=development
PORT=3000
APP_ORIGIN=http://localhost
SESSION_SECRET=replace-with-32-plus-char-random-secret

# PostgreSQL
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/poddashboard

# SMTP (Nodemailer)
SMTP_HOST=localhost
SMTP_PORT=1025
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
SMTP_FROM=Magic Pod Dashboard <no-reply@poddashboard.local>
